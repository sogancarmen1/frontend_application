import { ref } from 'vue'

export const taskIsSelected = ref(false)
export const projectIsSelected = ref(false)
export const dateIsSelected = ref(false)
export const priorityIsSelected = ref(false)
export const collaboratorIsSelected = ref(false)
export const statusIsSelected = ref(false)
